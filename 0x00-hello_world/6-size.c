#include <stdio.h>

/**
 * main - Entry point
 * Description: c program that prints the size of various types on the computer
 * Return: Always 0 (success)
*/

int main(void)
{   sizeof(char a);
	sizeof(int b);
return (0);
}
