#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: c program that prints random numbers
 *
 * Return: always 0 (success)
*/

int main(void)
{   int n=0;
if (n > 0)
printf("%i is positive", n)
else if (n < 0)
printf("%i is negative", n)
else
printf("%i is zero", n)
return (0);
}
