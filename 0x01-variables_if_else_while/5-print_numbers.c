#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: program that prints all single digit numbers of 10 start from 0
 *
 * Return: always 0 (success)
*/

int main(void)
{ int n;
while (n < 10)
printf("%i", n);
return (0);
}
