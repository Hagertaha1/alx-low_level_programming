#include <stdio.h>
#include "main.h"

/**
 * main _ Entry point function print_alphabet
 *
 * Description: make function that prints the alphabet in lowercase
 *
 * Return: 0 (success)
*/

int main(void)
{int ch;
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
return (0);
}
