#include <stdio.h>
#include "main.h"

/**
 * mul - function that multiplies two integers
 *
 * Description: multiplies two input a, b
 *
 * Retur: always 0 (success)
*/

int mul(int a, int b)
{
printf(%i, (a * b));
return (0);
}
